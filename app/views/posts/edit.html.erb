<%= form_for @post, url: {action: :update}, method: :patch do |p| %>
	<%= p.label :title %>
	<%= p.text_field :title %>

	<%= p.label :message %>
	<%= p.text_area :message %>

	<%= p.submit 'Salvar' %>
<% end %>